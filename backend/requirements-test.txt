# Dépendances de test pour le backend Alteris

# Framework de test
pytest==8.3.2
pytest-asyncio==0.24.0

# Client HTTP pour tests d'intégration FastAPI
httpx==0.27.0

# Coverage (optionnel)
pytest-cov==5.0.0

# Mock et utilitaires de test (inclus dans Python 3.3+)
# unittest.mock est déjà disponible

# Autres outils utiles pour les tests
pytest-xdist==3.6.1  # Pour exécuter les tests en parallèle
pytest-timeout==2.3.1  # Pour gérer les timeouts
